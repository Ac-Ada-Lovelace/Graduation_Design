# 数据采集后台功能讨论画板

## 目标
围绕“数据采集部分”的汇报，先把后台接收软件拆成可讨论、可落地、可交给 Codex 实现的功能点。

---

## 已确认设计决策
1. 总览页采用 **设备卡片墙** 作为主视图。
2. 单终端详情页默认主图指标选择 **A相功率**。
3. 协议包详情采用 **单独页面**，不使用抽屉或弹窗。
4. 必须支持 **真实 TCP 收包**，不能只做假数据演示。
5. 当前实现与后续延续都使用 **WPF**，但不背过重架构包袱。

---

## 一、页面层次设计

### 1. 总览页（多终端总视图）
**定位**：系统入口页，展示“当前系统正在接收多个终端数据”。

**核心目标**：
- 让老师一眼看到系统在运行
- 体现“多终端并行接入”
- 体现“实时性”
- 体现“协议数据已经被解析成业务数据”

**建议功能点**：
1. 监听状态卡片
   - 当前监听端口
   - 服务运行状态
   - 启动时间
2. 全局统计卡片
   - 在线终端数
   - 今日累计报文数
   - 每秒接收速率
   - 异常报文数
3. 终端概览区（主区域，设备卡片墙）
   - 每个终端一张卡片
   - 显示设备 ID
   - 在线/离线状态
   - 最近上报时间
   - A相功率当前值
   - 最近一组关键值摘要
4. 实时趋势区
   - 总览页不追求大面积图表墙
   - 可选展示少量微型趋势
   - 如展示图表，默认优先显示 A相功率
5. 实时数据滚动区
   - 最新解析结果列表
   - 按时间倒序滚动
   - 显示设备 ID、时间、主要字段、状态
6. 异常提醒区
   - 解析失败
   - 设备掉线
   - 数值越界

**展示重点**：
- 更偏“设备卡片墙 + 实时日志”的系统总控台
- 不追求复杂交互，追求“多终端在线运行感”

---

### 2. 单终端详情页
**定位**：展示单个终端在一段时间内的详细数据。

**核心目标**：
- 体现“设备级管理”
- 体现“时间序列数据已被组织起来”
- 体现“既能看实时值，也能看历史趋势”

**建议功能点**：
1. 终端基础信息
   - 设备 ID
   - 当前连接状态
   - 最近上报时间
   - 最近连接地址
   - 累计上报条数
2. 时序折线图
   - 默认显示 **A相功率**
   - 可预留切换为电流A、电压A、其他指标
   - 默认显示最近1分钟或最近5分钟
3. 实时数据表
   - 时间戳
   - 各字段解析值
   - 按新到旧排列
4. 状态事件区
   - 上线时间
   - 掉线时间
   - 重连记录
   - 异常数据记录
5. 原始报文入口
   - 点击某一条记录，可进入对应协议包详情页

**展示重点**：
- 这是“设备画像页”
- **A相功率时序图 + 实时数据表**是主角

---

### 3. 协议包详情页
**定位**：从“业务数据展示”下钻到“协议报文本身”。

**核心目标**：
- 证明系统确实按协议解析
- 能把原始字节、字段定义、业务值对应起来
- 支撑你对协议设计的讲解

**建议功能点**：
1. 包基础信息
   - 接收时间
   - 来源连接/设备 ID
   - 包长度
   - 解析状态
2. 原始数据展示
   - 十六进制视图
   - 可按4字节分组显示
3. 字段解析视图
   - 字段名
   - 类型
   - 字节偏移
   - 原始字节
   - 解析结果
4. 协议结构说明区
   - 设备 ID
   - 上报时间
   - 电流A/B/C
   - 电压A/B/C
   - 功率A/B/C
5. 解析映射说明
   - 报文片段如何映射为业务数据
6. 异常说明（可选）
   - 若某包解析失败，显示失败原因

**展示重点**：
- 更偏“协议教学页 / 调试页”
- 非常适合汇报时演示“真实收包 + 真正解析”

---

## 二、建议的页面关系

1. 总览页
   - 点击某个终端卡片 → 进入单终端详情页
   - 点击某条实时记录 → 进入协议包详情页
2. 单终端详情页
   - 点击某一条历史记录 → 进入协议包详情页
3. 协议包详情页
   - 可返回所属终端详情页
   - 可返回总览页

这套关系很清楚：
**系统级 → 设备级 → 报文级**

---

## 三、功能点优先级（给 Codex 的实现边界）

### P0：必须有
1. TCP 监听与真实收包
2. 协议解析
3. 多终端按 ID 区分
4. 总览页（设备卡片墙）
5. 单终端详情页
6. 协议包详情页（独立页面）
7. 实时列表刷新
8. 单终端 A相功率折线图

### P1：建议有
1. 在线/离线状态判断
2. 异常报文提示
3. 最近若干分钟缓存
4. 设备统计信息
5. 字段偏移说明

### P2：有时间再做
1. 图表切换多指标
2. 导出 CSV
3. 协议包搜索/筛选
4. 更完整的异常日志页
5. 历史回放

---

## 四、适合明天汇报的实现边界

### 推荐口径
本阶段目标不是做完整平台，而是完成一个可运行的数据接收与展示原型，重点验证：
- 多终端接入可行
- 协议解析可行
- 实时展示可行
- 设备级与报文级查看可行

---

## 五、实现难度判断（WPF + Codex）

### 1小时内较稳妥的范围
如果用 **WPF**，并且聚焦“可演示原型”，1小时内有机会完成：
- 主窗口总览页
- 设备卡片墙
- 实时滚动表格
- 单终端 A相功率简单折线图
- 协议包详情独立页面
- 真实收包 + 基础解析 + 刷新展示

### 1小时内风险较高的范围
- 真正完整的 TCP 多连接 + 实时线程安全刷新 + 三层页面都精致完成
- 复杂图表交互
- 完整协议异常处理
- 很成熟的 MVVM 分层

### 更现实的做法
优先做一个“可演示原型”：
1. 先保证真实 TCP 收包、协议解析、基础刷新链路跑通
2. UI 聚焦三层页面，不追求复杂美化
3. 协议包详情直接做独立页面
4. 图表只做最关键的一组，即单终端 A相功率

---

## 六、当前倾向方案

### 我建议的最低可演示版本
- **总览页**：设备卡片墙 + 全局统计 + 最新解析记录滚动表
- **单终端页**：A相功率图 + 最近数据表
- **协议包页**：原始十六进制 + 字段解析表 + 字段说明

这样已经足够支撑一次汇报。

---

## 七、待讨论问题
## 七、已落定的问题
1. 总览页采用 **设备卡片墙**。
2. 图表默认显示 **A相功率**。
3. 协议包详情采用 **单独页面**。
4. 为了汇报，必须做 **真实收包**，不是 UI 空壳。
5. WPF 不只是这次汇报的快速工具，后续也继续使用，但不背过重包袱。

## 八、下一步可继续细化的问题
1. 设备卡片上除了 A相功率，还要不要同时显示最近电压/在线时长？
2. 单终端详情页是否要加入“最近异常记录”独立区域？
3. 协议包详情页里，字段解释是放表格内还是放侧边说明区？
4. 是否需要在总览页加入“最近掉线设备”小区域？

