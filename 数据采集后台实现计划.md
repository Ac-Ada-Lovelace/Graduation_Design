# 数据采集后台实现计划

## 1. 当前目标
实现一个基于 WPF 的数据采集后台原型，用于毕业设计中的终端接入、协议解析、实时展示与报文级查看。

当前阶段优先保证：
- 真实 TCP 收包
- 协议解析正确
- 多终端按设备 ID 区分
- 总览页、单终端详情页、协议包详情页可演示
- 接收到的数据实时写入文件

当前阶段明确不做：
- 数据库
- 历史数据读取与查询
- 图表
- 重型架构和复杂权限系统

---

## 2. 已确认技术决策
- 桌面端框架：`.NET 8 WPF`
- 通信方式：`TCP Server`
- 协议版本：`电力数据量测上报协议 V1.0`
- 展示方式：第一版使用实时更新列表，不做图表
- 存储方式：文件追加写入，不做数据库
- 配置方式：使用 `appsettings.json`

---

## 3. 协议确认结果
根据协议文档，当前上报包采用固定长度定长结构：

- 总长度：`44` 字节
- 字节序：`little-endian`
- 结构：`2 x Int32 + 9 x Float32`

字段顺序如下：
1. `设备ID` `Int32`
2. `上报时间` `Int32`
3. `电流A` `Float32`
4. `电流B` `Float32`
5. `电流C` `Float32`
6. `电压A` `Float32`
7. `电压B` `Float32`
8. `电压C` `Float32`
9. `正向有功功率A` `Float32`
10. `正向有功功率B` `Float32`
11. `正向有功功率C` `Float32`

设备行为约束：
- 设备上电后主动连接平台
- 默认 `1 秒 1 包`
- 连接断开后自动重连

---

## 4. 第一版页面范围

### 4.1 总览页
展示系统当前整体运行状态。

包含内容：
- 监听状态
- 当前监听地址与端口
- 在线终端数
- 累计报文数
- 异常报文数
- 设备卡片墙
- 最新报文实时列表

### 4.2 单终端详情页
展示单个终端的当前状态与最近上报记录。

包含内容：
- 设备名称
- 设备 ID
- 在线状态
- 最近上报时间
- 最近连接地址
- 最新一组量测值
- 该终端的实时报文列表
- 状态事件列表

### 4.3 协议包详情页
展示单条报文的原始内容与解析映射。

包含内容：
- 接收时间
- 来源连接
- 原始十六进制
- 字段偏移
- 字段名
- 原始值
- 解析值
- 解析状态
- 异常原因（如有）

---

## 5. 存储方案
第一版采用文件追加写入，目标是保证每个真实接收到的报文都可留痕。

存储原则：
- 每收到一包就落盘
- 不要求当前版本支持历史读取
- 文件格式使用 `JSON Lines`（`.jsonl`）

选用 `JSON Lines` 的原因：
- 便于追加写入
- 同时保存原始报文和解析结果
- 便于后续调试与扩展

单条记录建议包含：
- `receivedAt`
- `remoteEndPoint`
- `deviceId`
- `deviceName`
- `reportTimeUtc`
- `currentA/B/C`
- `voltageA/B/C`
- `powerA/B/C`
- `rawHex`
- `parseStatus`
- `errorMessage`

---

## 6. 配置方案
使用 `appsettings.json` 管理监听参数、存储目录和设备映射。

建议配置结构：

```json
{
  "Listener": {
    "Host": "0.0.0.0",
    "Port": 5000,
    "OfflineTimeoutSeconds": 5
  },
  "Storage": {
    "Directory": "data",
    "FileNamePattern": "measurements-{yyyyMMdd}.jsonl"
  },
  "Devices": {
    "1": {
      "Name": "客厅总表"
    },
    "2": {
      "Name": "空调回路"
    }
  }
}
```

配置规则：
- `Devices` 节点使用设备 ID 作为键
- 每个设备至少配置 `Name`
- 未命中的设备使用回退名称，例如 `设备-<id>`

---

## 7. 预期项目结构
计划保持轻量结构，不引入过重框架，但保证后续可继续扩展。

建议目录：
- `Models`：协议模型、设备模型、报文模型
- `Services`：TCP 接收、协议解析、文件写入、设备状态聚合
- `ViewModels`：页面状态组织
- `Views`：WPF 页面与窗口
- `Configuration`：配置对象

---

## 8. 实施顺序

### 阶段一：基础链路
- 新建 `.NET 8 WPF` 项目
- 建立配置读取
- 建立 TCP 监听服务
- 按 `44` 字节定长读取协议包
- 完成协议解析
- 建立内存态设备聚合
- 建立 JSONL 文件落盘

### 阶段二：总览页
- 展示监听状态
- 展示全局统计
- 展示设备卡片墙
- 展示最新报文实时列表

### 阶段三：单终端详情页
- 展示设备基础信息
- 展示当前最新量测值
- 展示该设备报文列表
- 展示状态事件

### 阶段四：协议包详情页
- 展示原始十六进制
- 展示字段偏移与映射
- 展示解析结果
- 展示异常信息

### 阶段五：联调与演示准备
- 验证真实 TCP 收包
- 验证多终端区分
- 验证断线重连后的状态变化
- 验证落盘文件内容
- 验证页面刷新线程安全

---

## 9. 第一版验收标准
满足以下条件即可认为第一版完成：

- 程序能监听指定 TCP 端口
- 能持续接收真实终端上报
- 能按协议正确解析 `44` 字节数据包
- 能按设备 ID 聚合终端
- 能根据配置显示设备名称
- 能在总览页看到多终端与最新报文
- 能进入单终端详情页查看该设备报文
- 能进入协议包详情页查看原始报文与字段解析
- 每条接收报文都已成功写入文件

---

## 10. 当前不做的内容记录
以下内容保留到后续版本再决定：
- A 相功率趋势图
- 多指标切换
- 历史数据读取
- 数据导出
- 报文搜索与筛选
- 数据库接入
- 完整异常日志系统
