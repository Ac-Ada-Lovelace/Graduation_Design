<Window x:Class="GraduationDesign.App.Views.PacketDetailWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}"
        Width="1180"
        Height="800"
        MinWidth="1000"
        MinHeight="680"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Border Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="320" />
                </Grid.ColumnDefinitions>

                <StackPanel>
                    <TextBlock FontSize="24" FontWeight="SemiBold" Foreground="#FF173042" Text="{Binding DeviceText}" />
                    <TextBlock Margin="0,8,0,0" FontSize="13" Foreground="#FF173042" Text="{Binding ReceivedAtText, StringFormat=接收时间: {0}}" />
                    <TextBlock Margin="0,6,0,0" FontSize="13" Foreground="#FF173042" Text="{Binding ReportTimeText, StringFormat=上报时间: {0}}" />
                    <TextBlock Margin="0,6,0,0" FontSize="13" Foreground="#FF173042" Text="{Binding RemoteEndPoint, StringFormat=来源连接: {0}}" />
                </StackPanel>

                <StackPanel Grid.Column="1">
                    <TextBlock FontSize="13" Foreground="#FF587282" Text="解析状态" />
                    <TextBlock Margin="0,8,0,0" FontSize="22" FontWeight="SemiBold" Foreground="#FF173042" Text="{Binding ParseStatusText}" />
                    <TextBlock Margin="0,16,0,0" FontSize="13" Foreground="#FF587282" Text="异常说明" />
                    <TextBlock Margin="0,8,0,0" FontSize="13" Foreground="#FF173042" Text="{Binding ErrorMessage}" TextWrapping="Wrap" />
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="1" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#FF173042" Text="原始十六进制" />

                <ScrollViewer Grid.Row="1" Margin="0,14,0,0" VerticalScrollBarVisibility="Auto">
                    <TextBox Background="#FFF8FAFC"
                             BorderThickness="0"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             FontFamily="Consolas"
                             FontSize="14"
                             Text="{Binding RawHex, Mode=OneWay}" />
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Row="2" Style="{StaticResource CardBorderStyle}">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock FontSize="18" FontWeight="SemiBold" Foreground="#FF173042" Text="字段解析" />

                <DataGrid Grid.Row="1"
                          Margin="0,14,0,0"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding Fields}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Width="160" Header="字段名" Binding="{Binding Name}" />
                        <DataGridTextColumn Width="110" Header="类型" Binding="{Binding DataType}" />
                        <DataGridTextColumn Width="90" Header="偏移" Binding="{Binding Offset}" />
                        <DataGridTextColumn Width="90" Header="长度" Binding="{Binding Length}" />
                        <DataGridTextColumn Width="220" Header="原始字节" Binding="{Binding RawHex}" />
                        <DataGridTextColumn Width="*" Header="解析值" Binding="{Binding ParsedValue}" />
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Border>
    </Grid>
</Window>
