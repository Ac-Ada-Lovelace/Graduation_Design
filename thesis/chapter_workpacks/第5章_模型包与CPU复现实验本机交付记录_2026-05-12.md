# 第5章模型包与CPU复现实验本机交付记录

日期：2026-05-12

## 1. 结论

当前机器即 GPU 训练机。需求表中第 5/7 章 CPU 复现实验所需的 P0/P1 工件已经在本机找到，并完成本机 CPU 推理链路校验。

当前不建议直接把模型包和 CSV 强行提交进普通 Git 历史，因为相关目录已在 `stages/stage-02/.gitignore` 中标记为生成工件目录：

- `model/data/processed/`
- `model/artifacts/`
- `model/runs/`

如需远程交付，建议优先使用 Git LFS、GitHub Release artifact 或外部文件交付方式。

## 2. 已找到的核心工件

| 优先级 | 需求项 | 本机状态 | 本机路径 | 大小 |
| --- | --- | --- | --- | ---: |
| P0 | 标准模型包 event optimized | 已找到并校验 | `stages/stage-02/model/artifacts/packages/kmt_multi_w301_20260323_155405_thopt_20260323_161139__event_optimized.zip` | 58,423,057 bytes |
| P0 | 标准模型包 error optimized | 已找到 | `stages/stage-02/model/artifacts/packages/kmt_multi_w301_20260323_155405_calibrated_20260323_161136__error_optimized.zip` | 58,423,534 bytes |
| P0 | 推理/验收 CSV | 已找到 | `stages/stage-02/model/data/processed/house_1_1s_kmt/timeseries_1s_train_ready.csv` | 30,358,156 bytes |
| P0 | 完整处理后 CSV | 已找到 | `stages/stage-02/model/data/processed/house_1_1s_kmt/timeseries_1s_full.csv` | 34,054,665 bytes |
| P1 | event optimized 验收报告 | 已找到 | `stages/stage-02/model/runs/acceptance_20260323_164509/report.json` | 4,223 bytes |
| P1 | error optimized 验收报告 | 已找到 | `stages/stage-02/model/runs/acceptance_20260323_164303/report.json` | 4,297 bytes |

## 3. 模型包与 CSV 字段校验

event optimized 模型包内包含：

```text
interface_spec.json
model.onnx
model_meta.json
normalization.json
onnx_parity_check.json
package_manifest.json
postprocess.json
threshold_optimization_report.json
```

模型元数据：

| 字段 | 值 |
| --- | --- |
| interface version | `nilm_model_interface_v1` |
| model name | `seq2point-kmt_multi_w301_20260323_155405` |
| dataset | `uk-dale` |
| sample period | `1.0s` |
| window size | `301` |
| appliances | `kettle`, `microwave`, `toaster` |
| input | `mains_window` |
| output | `pred_power` |

CSV 字段满足需求表：

```text
epoch_s
timestamp_utc
mains_w
kettle_w
microwave_w
toaster_w
```

## 4. 已执行的本机 CPU 校验

### 4.1 active package 校验

命令：

```powershell
python integration\manage_package_registry.py verify-active
```

结果：

```text
active_package=artifacts/packages/kmt_multi_w301_20260323_155405_thopt_20260323_161139__event_optimized.zip
resolved_path=D:\repos\Graduation_Design\stages\stage-02\model\artifacts\packages\kmt_multi_w301_20260323_155405_thopt_20260323_161139__event_optimized.zip
exists=True
```

### 4.2 API 检查

命令：

```powershell
python integration\run_service_api_checks.py
```

结果：

```text
[api-check] PASS (session + offline + online)
```

### 4.3 在线 smoke

命令：

```powershell
python integration\run_demo_smoke.py --data-csv model\data\processed\house_1_1s_kmt\timeseries_1s_train_ready.csv --speed 200 --max-rows 320
```

关键结果：

```text
[smoke] service is healthy
[smoke] session started: model=seq2point-kmt_multi_w301_20260323_155405 window=301
[smoke] latest ready=True pred_count=20
[smoke] PASS
```

最新在线输出样例：

```json
{
  "ready": true,
  "timestamp_utc": "2013-03-17T19:18:21Z",
  "pred_w": {
    "kettle": 0.0,
    "microwave": 0.0,
    "toaster": 0.9050426483154297
  },
  "model_version": "kmt_multi_w301_20260323_155405_thopt_20260323_161139__event_optimized.zip",
  "buffer_fill": 301,
  "window_size": 301,
  "ingest_count": 320,
  "pred_count": 20
}
```

## 5. 可写入论文的指标依据

event optimized 验收报告：

| 指标 | 值 |
| --- | ---: |
| MAE avg | 11.2317 W |
| RMSE avg | 44.7314 W |
| Event F1 avg | 1.0000 |

error optimized 验收报告：

| 指标 | 值 |
| --- | ---: |
| MAE avg | 12.9034 W |
| RMSE avg | 62.7407 W |
| Event F1 avg | 0.8333 |

## 6. 后续交付建议

1. 小文件可直接提交：本交付记录、需求表状态更新、验收报告摘要。
2. 大文件建议单独交付：两个模型包 zip、`timeseries_1s_train_ready.csv`、必要时附 `timeseries_1s_full.csv`。
3. 如果必须放入 GitHub 仓库，建议启用 Git LFS 后再纳入模型包和 CSV。
