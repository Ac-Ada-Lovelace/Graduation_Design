# 第7章 工作拆解与行文检查

日期：2026-05-12

## 1. 写作目标

第 7 章应写成测试与结果分析章，而不是实现章。每一节都应尽量包含测试输入、测试步骤、预期结果、实际结果和简短分析。

## 2. 小节拆解

### 7.1 测试环境与数据说明

必写：

- 本机 Windows CPU 环境。
- Python venv 和 Stage-02 service/model 依赖。
- 模型包路径、CSV 路径、数据范围、设备列表。
- 说明 GPU 机器负责训练，本机负责 CPU 推理复现。

### 7.2 数据采集模块测试

复用第 4 章证据，不重新跑：

- 常规多设备联调。
- 接收端压力联调。
- JSONL 落盘和状态分布。

### 7.3 推理服务功能测试

引用：

- `verify-active.txt`
- `api-check.txt`
- `smoke.txt`

写法重点：

- 证明模型包存在且可加载。
- 证明 session、offline、online 接口链路通过。
- 证明在线回放后服务能进入 ready 状态并产生预测。

### 7.4 模型识别与误差指标分析

引用：

- `stages/stage-02/model/runs/acceptance_20260323_164509/report.md`
- `stages/stage-02/model/runs/acceptance_20260323_164303/report.md`
- `offline-infer-summary.json`

写法重点：

- 先给总表，再分析 event optimized 和 error optimized 的差异。
- 不夸大模型泛化能力，强调当前指标来自固定区间验收和本机 CPU 复现。

### 7.5 在线回放与端到端测试

引用：

- `smoke.txt`
- `online-status-summary.json`

写法重点：

- 解释 `window_size=301`，回放 320 行产生 20 次预测。
- 解释服务端在线回放样例中 420 行产生 120 次预测。
- 为第 6 章 UI 展示留接口衔接。

### 7.6 测试结果讨论

分析成功点和局限：

- 采集链路、推理链路和回放链路都具备可运行证据。
- 模型包可以在非 GPU 机器上用 CPU 推理。
- 真实硬件长期运行、跨主机网络和真实家庭泛化仍是后续改进。

## 3. 成稿检查项

- [x] Stage-02 模型包已补齐。
- [x] 处理后 CSV 已补齐。
- [x] acceptance report 已补齐。
- [x] `verify-active` 已保存输出。
- [x] `run_service_api_checks.py` 已保存输出。
- [x] `run_demo_smoke.py` 已保存输出。
- [x] 离线推理样例 JSON 已保存。
- [x] 在线回放样例 JSON 已保存。
- [ ] 第 6 章 UI 截图尚未整理。
- [x] 第 7 章正文草稿已生成。
